import PropTypes from "prop-types";

const Credit = ({ setPaymentModal, balanceName }) => {
  return <div>Kredi</div>;
};

export default Credit;

Credit.propTypes = {
  setPaymentModal: PropTypes.func,
  balanceName: PropTypes.string,
};
